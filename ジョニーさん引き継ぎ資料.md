# LOVEQUEST MASTERS 開発引き継ぎ資料

作成日: 2026-08-04
作成: リョータロー（Claude Codeで開発）
引き継ぎ先: ジョニーさん

---

## 0. まず読んでほしいこと（重要）

現状、以下は**すべてリョータローの個人アカウント**で動いています。実運用に入る前に、**アカウントの所有権をジョニーさん側に移す（または新規にジョニーさん名義で作り直す）ことを強く推奨**します。理由は、API利用料の請求先・GitHubリポジトリの管理権限・Vercelの管理権限が全部リョータロー個人に紐づいているためです。

移管が必要なもの：
1. Anthropic APIキー（課金の起点）→ ジョニーさん名義のAnthropicアカウントで再発行
2. Vercelプロジェクト → ジョニーさん名義のアカウントに移管 or 招待
3. GitHubリポジトリ（2つ）→ ジョニーさん名義のアカウントに移管 or Organizationへ招待

やり方は「6. アカウント移管の進め方」に記載しています。

---

## 1. 全体像（できているもの一覧）

| # | 名称 | 内容 | URL |
|---|------|------|-----|
| 1 | RPGダッシュボードモック | アバター選択・ステータス・クエストのUIモック | https://fxsurfer.github.io/lovequest-8f3k2p/ |
| 2 | プレイヤー図鑑 | 16タイプ×256組み合わせの相性を自動計算表示（アルゴリズム生成） | https://fxsurfer.github.io/lovequest-8f3k2p/player_encyclopedia.html |
| 3 | タイプ診断フローチャート | 太陽/月→外向/内向→職業確認の3ステップ診断ツール | https://fxsurfer.github.io/lovequest-8f3k2p/type_diagnosis_flowchart.html |
| 4 | 相性ライブラリ | ジョニーさん執筆の本物の相性解説125/136通りを閲覧できるツール（AI川瀬風UI） | https://fxsurfer.github.io/lovequest-8f3k2p/compatibility_library.html |
| 5 | 提案書PDF | LOVEQUEST MASTERS構築提案書（常に最新版が同じURLで見られる） | https://fxsurfer.github.io/lovequest-8f3k2p/proposal.pdf |
| 6 | キャラデザインブリーフ | 16タイプ分のイラスト発注用デザイン仕様書 | リポジトリ内 `character_design_brief_16types.md` |
| 7 | **AIジョニー** | 写真・動画・テキストをアップロードすると実際にAIがタイプ診断してくれる本番稼働中のツール | https://ai-johnny-backend.vercel.app |

1〜6はGitHub Pages（無料・静的ホスティング）、7だけVercel（サーバー機能あり・従量課金）で動いています。

---

## 2. リポジトリとソースファイルの場所

### 2-1. 静的サイト一式（1〜6番）
- GitHubリポジトリ: `fxsurfer/lovequest-8f3k2p`（Public、`noindex`設定済みなので検索には出ません）
- ローカルの元ファイル: `C:\Users\ryota\OneDrive\MEGA移行\Dropbox\Private\恋愛\BFMブラメン\LOVEQUEST\`
- 更新方法: このフォルダ内のファイルを編集 → リポジトリにコピー → `git push` すると数分でサイトに反映されます

### 2-2. AIジョニー バックエンド（7番）
- GitHubリポジトリ: `fxsurfer/ai-johnny-backend`（**Private**、APIキーを扱うため非公開にしています）
- 構成:
  ```
  ai-johnny-backend/
  ├── api/analyze.js      ← Claude Vision APIを呼び出すサーバーレス関数
  ├── public/index.html   ← フロントエンド（AI川瀬風チャットUI）
  └── package.json
  ```
- ホスティング: Vercel（チーム名: `lovequest-masters`）
- `git push`すると自動的にVercelが再デプロイします（GitHub連携済み）

---

## 3. AIジョニーの仕組みと費用

### 仕組み
```
ユーザーがブラウザで写真/動画/テキストを入力
   ↓
/api/analyze （Vercelのサーバーレス関数）にPOST送信
   ↓
サーバー側でClaude(Anthropic)のVision APIを呼び出す
   ↓
診断結果(タイプ診断・相性・デートプラン・攻略ヒント)を返す
   ↓
チャット画面に表示
```
APIキーはVercelの環境変数（`ANTHROPIC_API_KEY`）に保存されており、ブラウザ側からは見えない安全な設計になっています。

### 費用の実績値
- テキストのみの診断: 約3円/回
- 写真5枚を使った診断: 約15〜25円/回
- $20のクレジットで、写真ありの診断が**200〜400回分**使える計算

### 動画対応について
動画そのものはAIに送れないため、ブラウザ側で動画から3フレームを静止画として自動抽出し、画像として送信する仕組みになっています。

---

## 4. データの出典と未完成部分

### 相性ライブラリ（4番）の元データ
ジョニーさんが用意したGoogle Docs（16タイプ組み合わせ資料）から、136通りの組み合わせのうち **125通り** を自動抽出して取り込み済みです。

**まだ書かれていない11通り**（Google Docsに存在しない組み合わせ）:
```
狂戦士×狂戦士 / 狂戦士×聖戦士 / 魔法戦士×魔法戦士 / 聖戦士×聖戦士 /
聖戦士×魔道士 / シスター×魔道士 / 忍者×魔道士 / 忍者×忍者 /
占い師×忍者 / 道化師×道化師 / 占い師×占い師
```
これらが後日Google Docsに追加されたら、同じ手順（Docsをエクスポート→パース→JSON更新）で反映できます。ツール側は自動で「執筆中」表示から本文表示に切り替わります。

### AIジョニーの出力フォーマットのブレ
AIの返答は「🔮タイプ診断／💞相性の見立て／🍽おすすめデートプラン／🛡攻略のヒント」の4見出しで固定するようプロンプト指定していますが、AIが自主的に小見出し（📸写真から読み取れる印象、等）を追加することがあります。気になる場合は`api/analyze.js`内の`SYSTEM_PROMPT`を調整することで改善できます。

---

## 5. 今後の検討事項（未着手）

1. **UTAGE連携**：会員費を払った人だけがAIジョニーを使えるように、アクセスコード方式（AI川瀬と同じ仕組み）を導入する
2. **利用上限の設計**：1人あたり月◯回まで、という制限をどう実装するか（現状は誰でも無制限に使える状態なので、公開する前に対策が必要）
3. **残り11通りの相性解説の執筆**
4. **AIジョニーの出力フォーマットの安定化**

---

## 6. アカウント移管の進め方

### GitHubリポジトリの移管
1. `fxsurfer/lovequest-8f3k2p` と `fxsurfer/ai-johnny-backend` それぞれの Settings → General → Danger Zone → **Transfer ownership**
2. ジョニーさんのGitHubアカウント名を入力して移管

または、リョータローのアカウントのまま「Collaborator（共同管理者）」としてジョニーさんを招待する形でも運用可能です（この場合、リョータローのアカウントに依存が残ります）。

### Vercelプロジェクトの移管
1. Vercelの「lovequest-masters」チームの Settings → Members からジョニーさんを招待
2. または、プロジェクトごとジョニーさんの新しいVercelアカウントに Transfer

### Anthropic APIキーの移管
APIキー自体の「移管」はできないため、**ジョニーさん名義でAnthropicアカウントを新規作成し、新しいAPIキーを発行**→Vercelの環境変数を新しいキーに差し替える、という形になります。旧キーは無効化してOKです。

---

## 7. 困ったときの参考情報

- GitHub Pagesの反映が遅い/404になる場合: 空コミットしてpushし直すと再ビルドがかかることが多い
- Vercelのビルドエラーは、プロジェクトの「Deployments」タブから該当のデプロイをクリックするとログが見られる
- Google Docsの新しい相性解説を追加でパースしたい場合の手順は、開発時の会話ログ（Claude Codeのセッション履歴）に詳細な手順が残っています
